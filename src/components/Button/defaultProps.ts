import { TdButtonProps } from './type';

export const buttonDefaultProps: TdButtonProps = {
  block: false,
  disabled: false,
  form: undefined,
  ghost: false,
  loading: false,
  shape: 'rectangle',
  size: 'medium',
  type: 'button',
  variant: 'base',
};
