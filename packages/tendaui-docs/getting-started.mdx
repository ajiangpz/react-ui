import { Meta } from "@storybook/blocks";

<Meta title="文档/快速开始" />

# 快速开始

本指南将帮助你在项目中快速上手 TendaUI React 组件库。

## 📋 前置要求

在开始之前，请确保你的开发环境满足以下要求：

- **Node.js**: >= 18.0.0
- **React**: >= 16.13.1
- **TypeScript**: >= 4.5.0（推荐）

## 🚀 安装

### 1. 创建 React 项目

如果你还没有 React 项目，可以使用以下命令创建：

#### 使用 Vite（推荐）

```bash
npm create vite@latest my-app -- --template react-ts
cd my-app
```

#### 使用 Create React App

```bash
npx create-react-app my-app --template typescript
cd my-app
```

### 2. 安装 TendaUI

在项目目录中运行以下命令安装 TendaUI：

```bash
# 使用 npm
npm install tendaui-react

# 使用 pnpm
pnpm add tendaui-react

# 使用 yarn
yarn add tendaui-react
```

### 3. 安装图标库（可选）

如果需要使用图标组件：

```bash
npm install tendaui-react-icons
```

## 💡 基础用法

### 完整引入

在 `main.tsx` 或 `App.tsx` 中引入样式：

```tsx
import React from "react";
import ReactDOM from "react-dom/client";
import App from "./App";

// 引入 TendaUI 样式
import "tendaui-react/es/style/index.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
```

然后就可以在组件中使用：

```tsx
import React from "react";
import { Button, Input, Space } from "tendaui-react";
import { IconPlay } from "tendaui-react-icons";

function App() {
  const [value, setValue] = React.useState("");

  return (
    <div style={{ padding: "24px" }}>
      <Space direction="vertical" size="large">
        <Button type="primary" icon={<IconPlay />}>
          开始使用
        </Button>

        <Input placeholder="请输入内容" value={value} onChange={(e) => setValue(e.target.value)} />
      </Space>
    </div>
  );
}

export default App;
```

### 按需引入

如果你想减小打包体积，可以按需引入组件：

```tsx
// 只引入需要的组件
import Button from "tendaui-react/es/button";
import Input from "tendaui-react/es/input";

// 引入对应的样式
import "tendaui-react/es/button/style";
import "tendaui-react/es/input/style";

function App() {
  return (
    <div>
      <Button type="primary">按钮</Button>
      <Input placeholder="输入框" />
    </div>
  );
}
```

## 🎨 配置主题

TendaUI 提供了灵活的主题定制能力。

### 方式一：使用 ConfigProvider

```tsx
import React from "react";
import { ConfigProvider, Button } from "tendaui-react";

function App() {
  return (
    <ConfigProvider
      theme={{
        primaryColor: "#1890ff"
      }}
    >
      <Button type="primary">自定义主题按钮</Button>
    </ConfigProvider>
  );
}
```

### 方式二：覆盖 CSS 变量

在你的全局样式文件中：

```css
/* styles/custom-theme.css */
:root {
  /* 主色调 */
  --td-brand-color: #1890ff;
  --td-brand-color-hover: #40a9ff;
  --td-brand-color-active: #096dd9;

  /* 功能色 */
  --td-success-color: #52c41a;
  --td-warning-color: #faad14;
  --td-error-color: #ff4d4f;

  /* 文本色 */
  --td-text-color-primary: rgba(0, 0, 0, 0.9);
  --td-text-color-secondary: rgba(0, 0, 0, 0.6);
  --td-text-color-placeholder: rgba(0, 0, 0, 0.4);

  /* 边框 */
  --td-border-radius: 4px;
  --td-border-color: #d9d9d9;
}
```

然后在入口文件引入：

```tsx
import "./styles/custom-theme.css";
import "tendaui-react/es/style/index.css";
```

## 📱 响应式设计

TendaUI 组件支持响应式设计，你可以轻松构建适配各种设备的应用：

```tsx
import { Grid, Button } from "tendaui-react";

function ResponsiveLayout() {
  return (
    <Grid container spacing={2}>
      <Grid item xs={12} sm={6} md={4}>
        <Button block>按钮 1</Button>
      </Grid>
      <Grid item xs={12} sm={6} md={4}>
        <Button block>按钮 2</Button>
      </Grid>
      <Grid item xs={12} sm={6} md={4}>
        <Button block>按钮 3</Button>
      </Grid>
    </Grid>
  );
}
```

## 🔧 TypeScript 支持

TendaUI 使用 TypeScript 编写，提供完整的类型定义：

```tsx
import React from "react";
import { Button, ButtonProps } from "tendaui-react";

// 类型会自动推导
const CustomButton: React.FC<ButtonProps> = (props) => {
  return <Button {...props} />;
};

function App() {
  // 编辑器会提供完整的类型提示
  return (
    <CustomButton
      type="primary"
      size="large"
      onClick={(e) => {
        console.log("clicked", e);
      }}
    >
      点击我
    </CustomButton>
  );
}
```

## 📦 项目结构建议

推荐的项目结构：

```
my-app/
├── src/
│   ├── components/          # 自定义组件
│   │   ├── Header/
│   │   └── Footer/
│   ├── pages/              # 页面组件
│   │   ├── Home/
│   │   └── About/
│   ├── styles/             # 全局样式
│   │   ├── custom-theme.css
│   │   └── global.css
│   ├── App.tsx
│   └── main.tsx
├── package.json
└── tsconfig.json
```

## 🎯 下一步

现在你已经完成了基础配置，可以：

1. 📖 浏览 [组件文档](/docs/components-button--docs)，了解每个组件的详细用法
2. 🎨 查看 [设计规范](/docs/design-tokens)，了解设计系统
3. 💡 参考 [最佳实践](/docs/best-practices)，提升开发效率
4. 🤝 阅读 [贡献指南](/docs/contributing)，参与项目贡献

## ❓ 常见问题

### 如何解决样式不生效？

确保正确引入了样式文件：

```tsx
import "tendaui-react/es/style/index.css";
```

### 如何实现按需加载？

使用 ES Module 的 tree-shaking 特性，配合现代打包工具（Vite、Webpack 5+）自动实现按需加载：

```tsx
// ✅ 推荐：直接从主包引入
import { Button, Input } from "tendaui-react";

// ❌ 不推荐：手动引入子路径
import Button from "tendaui-react/es/button";
```

### 如何在 Next.js 中使用？

在 `_app.tsx` 中引入样式：

```tsx
import "tendaui-react/es/style/index.css";
import type { AppProps } from "next/app";

export default function App({ Component, pageProps }: AppProps) {
  return <Component {...pageProps} />;
}
```

### 遇到问题怎么办？

1. 查看 [FAQ 文档](/docs/faq)
2. 在 [GitHub Issues](https://github.com/your-repo/tendaui/issues) 搜索相似问题
3. 提交新的 Issue

---

<div
  style={{
    padding: "16px",
    background: "#f0f9ff",
    border: "1px solid #91caff",
    borderRadius: "8px",
    marginTop: "32px"
  }}
>
  <strong>💡 提示：</strong> 如果你在使用过程中遇到任何问题，欢迎在 GitHub 上提出 Issue 或加入我们的社区讨论。
</div>
